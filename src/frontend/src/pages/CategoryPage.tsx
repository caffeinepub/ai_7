// This page is no longer used - the app now uses tag-based filtering on the homepage.
// Keeping this file to avoid broken imports, but it renders nothing meaningful.
export function CategoryPage() {
  return null;
}
